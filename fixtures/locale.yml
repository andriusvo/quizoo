sylius_fixtures:
    suites:
        default:
            fixtures:
                app_locale:
                    priority: 0
                    options:
                        locales:
                            - { code: en }
